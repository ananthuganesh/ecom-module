{
  "name": "siyara",
  "version": "2.0.0",
  "private": true,
  "description": "Urban Aana ecommerce — Next.js web + FastAPI api + MongoDB",
  "scripts": {
    "install:all": "npm install --prefix apps/web && cd apps/api && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "dev:api": "cd apps/api && .venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "dev:web": "npm run dev --prefix apps/web",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev:api\" \"npm run dev:web\"",
    "test:api": "cd apps/api && .venv/bin/pytest -q",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
